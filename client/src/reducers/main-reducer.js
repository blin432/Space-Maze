
let initState = {

}

export const mainReducer = (state = initState, action) => {
    switch(action.type) {
        case "": 
        return {
        }
        case "":
        return {
        }
        default:
        return state;
    }
}